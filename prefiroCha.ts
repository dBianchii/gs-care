class MotivosParaOdiarJava {
  static porqueJava() {
    console.log(
      "Java: Transformando cafés rápidos em longas sestas desde 1995."
    );
  }

  static atualizacoesInfinitas() {
    console.log(
      "A única certeza na vida é a morte, impostos, e uma nova atualização do Java."
    );
  }

  static gerenciamentoDeMemoriaHistorico() {
    console.log(
      "Java faz o gerenciamento de memória parecer um episódio de 'Hoarders'."
    );
  }

  static campeonatoDeVelocidade() {
    console.log(
      "Numa corrida entre um programa Java e uma tartaruga, aposte na tartaruga."
    );
    console.log(
      "Sóq nao, todo mundo sabe que JS é mais lento que Java. Nao conta pra ninguem"
    );
  }

  static festivalDeExcecoes() {
    console.log(
      "Java: Onde até um 'Olá, Mundo!' pode lançar uma NullPointerException."
    );
  }

  static verbosidadeExtrema() {
    console.log(
      "Java, porque por que dizer em uma linha o que você pode dizer em vinte?"
    );
  }

  static portabilidadeQuestionavel() {
    console.log("Java: Escreva uma vez, depure em qualquer lugar.");
  }

  static sintaxe() {
    console.log(
      "A sintaxe do Java foi claramente inspirada por um labirinto. Sem saída."
    );
  }
}

MotivosParaOdiarJava.porqueJava();
MotivosParaOdiarJava.atualizacoesInfinitas();
MotivosParaOdiarJava.gerenciamentoDeMemoriaHistorico();
MotivosParaOdiarJava.campeonatoDeVelocidade();
MotivosParaOdiarJava.festivalDeExcecoes();
MotivosParaOdiarJava.verbosidadeExtrema();
MotivosParaOdiarJava.portabilidadeQuestionavel();
MotivosParaOdiarJava.sintaxe();

async function FlexingOnJava() {
  let inferencia = "inferido automaticamente como string";

  const array = [1, 2, 3, 4, 5];
  const dobrados = array.map((item) => item * 2);

  const linguagem = "TypeScript";
  console.log(`Template Strings: ${linguagem} é mais flexível que Java`);

  const objeto = { a: 1, b: 2, c: 3 };
  const { a, ...resto } = objeto;
  console.log(`Desestruturação e Spread Operator: Resto =`, resto);

  const exemploAsync = async () => {
    return "Resultado de uma operação assíncrona";
  };

  exemploAsync().then((resultado) => console.log(`Async/Await: ${resultado}`));
}

FlexingOnJava();
